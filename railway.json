{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS"
    },
    "deploy": {
        "startCommand": "cd UNIT-PROJECT-2 && python manage.py migrate && python manage.py collectstatic --noinput && gunicorn Trips.wsgi"
    }
}